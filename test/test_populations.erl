%%%-------------------------------------------------------------------
%%% @author borja
%%% @doc
%%%
%%% @end
%%%-------------------------------------------------------------------
-module(test_populations).
-compile([export_all, nowarn_export_all]). %%TODO: To delete after build

%% API 
% -export([]).


%%%===================================================================
%%% Defined agents
%%%===================================================================

% --------------------------------------------------------------------
n5_infinity() -> 
    #{max_size => 5}.


% --------------------------------------------------------------------
n5_100ms() -> 
    #{
        max_size  => 5,
        stop_time => 100
    }.

% --------------------------------------------------------------------
n5_10generations() -> 
    #{
        max_size  => 5,
        generations => 100
    }.

% --------------------------------------------------------------------
n5_1000points() -> 
    #{
        max_size  => 5,
        target => 1000
    }.

